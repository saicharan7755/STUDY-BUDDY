#!/usr/bin/env node

/**
 * Vercel API functions already exist in /api directory.
 * This script is kept as a no-op to avoid breaking the build command.
 */

console.info('API functions already in place. Skipping generation.');